import {getCurrentlyPLaying} from '/backend/AccessSpotify';

console.log(getCurrentlyPLaying);

function Header(){
    return(
        <header>
            I am real header
        </header>
    )
}

export default Header;